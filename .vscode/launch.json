{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Governor",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/src/governor.py",
            "cwd": "${workspaceFolder}/src"
        },
    ]
}
